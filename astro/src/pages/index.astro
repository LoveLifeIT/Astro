---
import "../static_hot_reload.js";
import Layout from '../layouts/Layout.astro';
import {getPageBySlug} from "../lib/api";

let page = await getPageBySlug("/");
if(!page) page = await getPageBySlug("/home");

---

{!!(page) && <Layout page={page} />}